<div class="profile-body">
    <div class="profile-wraper">
      <div class="profile-info">
        <%- include ("./layouts/info.ejs") %>
        <hr />
  
        <%- include ("./layouts/menu.ejs") %>
      </div>
      <div class="profile-box">
        <div class="profile-details">
          <div class="profile-photo">
            <% if (singleUser.photo) { %>
            <img
              class="profile-preview"
              src="/public/media/users/<%= singleUser.photo %>"
              alt=""
            />
            <% } else { %>
            <img class="profile-preview" src="/images/avatar.png" alt="" />
            <% } %>
            <h3><%= singleUser.name %></h3>
            <p><%= singleUser.email %></p>
            <p class="ami"><%= singleUser.followers.length %> followers.<%= singleUser.following.length %> following</p>
          </div>
  
          <div class="user-gallery">
            <h3>Gallery photo</h3>
            <hr />
            <div class="gallery-area">
              <% singleUser.gellary.forEach(element => { %>
              <div class="gallery-item">
                <img src="../public/media/gallery/<%= element %> " alt="" />
              </div>
  
              <% }) %>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
  